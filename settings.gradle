pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
        maven {
            url 'https://api.mapbox.com/downloads/v2/releases/maven'
            authentication {
                basic(BasicAuthentication)
            }
            credentials {
                // Do not change the username below.
                // This should always be `mapbox` (not your username).
                username ="mapbox"
                password = "sk.eyJ1IjoiZGV2bWlrZTAxIiwiYSI6ImNsY2twcnZhNDBiMTczc25yMmprbHdpenIifQ.ft-OGv_RO3M-bK_JnekMig"
                // it.username("mapbox")
                // Use the secret token you stored in gradle.properties as the password
                // it.password("sk.eyJ1IjoiZGV2bWlrZTAxIiwiYSI6ImNsY2twcnZhNDBiMTczc25yMmprbHdpenIifQ.ft-OGv_RO3M-bK_JnekMig")
            }
        }
    }
}
rootProject.name = "MapboxAutoCompleteTextField"
include ':app'
include ':mapboxautocomplete'
